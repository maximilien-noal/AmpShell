<Window x:Class="AmpShell.WPF.Views.CategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AmpShell.WPF"
        mc:Ignorable="d"
        Title="Adding/Editing a category..."
        SizeToContent="WidthAndHeight"
        ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterOwner">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}" />
    </Window.Style>
    <StackPanel>
        <Label Content="Please enter a name :" />
        <TextBox Text="{Binding Title}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="5" x:Name="OK" IsDefault="True">OK</Button>
            <Button Margin="5" x:Name="Cancel" IsCancel="True">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>